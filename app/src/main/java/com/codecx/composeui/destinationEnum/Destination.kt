package com.codecx.composeui.destinationEnum
/**
 * Trieda Enum, pre jednotlivé destinácie, ktoré sa používajú v navigácii
 * */
enum class Destination {
    Splash, Login, SignUp, Home, Search, Care, HomeMain, Timer, Planes,Subscription, Clients,ClientInfo, WorkOuts, Lists;
}